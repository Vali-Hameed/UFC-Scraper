name: ufc-scraper
services:
  scraper:
    build: .
    env_file: .env
    ports:
      - "8001:8000"
    environment:
      - BACKEND_BASE_URL=http://host.docker.internal:8080
      - SCRAPER_API_KEY=change-me
      - SCRAPER_CRON=0 2 * * *
